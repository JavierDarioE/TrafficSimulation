package empresa.TrafficSimulation.Simulacion

object Simulacion extends Runnable{
  def run(): Unit = {
    while (true) {
      //t += td
      //Grafico.graficar
      Thread.sleep(1000)
    }
  }
}